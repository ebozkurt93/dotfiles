#!/usr/bin/env bash

set -euo pipefail

if command -v hyprlock >/dev/null 2>&1; then
  exec hyprlock
fi

if command -v loginctl >/dev/null 2>&1; then
  exec loginctl lock-session
fi

echo "missing lock command: install hyprlock or provide loginctl" >&2
exit 1
